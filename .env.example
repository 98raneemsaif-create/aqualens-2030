# Optional host ports; all published ports bind only to localhost.
AIRFLOW_PORT=8080
KAFKA_HOST_PORT=9092

# Required only for live Gemini grounded generation / a full success DAG run.
# Keep the real value in your local .env only. Never commit it.
GEMINI_API_KEY=
